language: java
jdk:
  - openjdk17
branches:
  only:
    - main

# Travis CI Server Home
cache:
  directories:
    - "$HOME/.m2/repository"
    - "$HOME/.gradle"
before_install:
  - chmod +x gradlew
script:
  - ./gradlew clean build

# mail alert
notifications:
  email:
    recipients:
      - bingo992000@gmail.com
